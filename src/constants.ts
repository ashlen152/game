const pingIntervalTime = 2 * 1000;
const pingTimeout = 5 * 1000;
const syncIntervalPlayerTime = 15;

// Player
const SPEED = 5;

export {
  pingIntervalTime,
  pingTimeout,
  syncIntervalPlayerTime,
  SPEED
}

